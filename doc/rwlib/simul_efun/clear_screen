名稱：
	clear_screen() - 清除 title_screen 上的訊息。
語法：
	varargs void clear_screen(object me, int line);
用法：
	清除 title_screen 上的訊息，若未指定 line，則清除 title_screen 上所
	有訊息，否則只會清楚第 line 行的訊息，clear_screen 並不會關閉 
	title_screen。
參考：
	startup_title_screen, spec_line_msg, reset_screen
作者：
	Clode@RevivalWorld
