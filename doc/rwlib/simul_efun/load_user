名稱：
	load_user() - 載入或取得使用者物件。
語法：
	object load_user(string	id);
用法：
	依照 find_player(id)，load_object(id) 等兩個順序進行取得使用
	者物件的動作，若完全無法載入物件的則回傳 0 值，需注意此函數
	可直接載入非互動物件，也就是此物件被載入後若其玩家不在線上，
	則應該在程式結束前將其摧毀。
參考：

作者：
	Clode@RevivalWorld
