名稱：
	probability() - 依照機率選出目標。
語法：
	mixed probability(mapping m);
用法：
	依照 m 內所設定之機率選出所對應的 key。
	
	ex:
		m = ([ 
			"A" : 1,
		       	"B" : 3,
		       	this_player() : 4.
		    ]);
		    
	則 probability(m) 回傳的結果
	1/8 的機率會是 "A"
	3/8 的機率會是 "B"
	4/8 的機率會是 this_player()

參考：

作者：
	Clode@RevivalWorld