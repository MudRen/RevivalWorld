名稱：
	arrange_city_location() - 回傳一個標準城市座標。
語法：
	array arrange_city_location(int x, int y, string city, int num);
用法：
	經由輸入四個基本參數，回傳一個標準的地圖座標格式陣列。
參考：
	load_cityroom, city_roomfiles, city_coor_short
作者：
	Clode@RevivalWorld
