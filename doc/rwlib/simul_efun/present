名稱：
	present() - 回傳符合輸入條件的物件。
語法：
	varargs	object present(	string str, object ob );
用法：
	根據字串 str 的內容，傳回物件 ob 的內容物中符合 str 條件的物件，
	所謂的符合條件代表 id(str) 回傳非零值，若未輸入 ob 參數，則會依序
	搜尋 this_player() 的內容物與 environment(this_player()) 的內容物。
參考：
	present_objects
作者：
	Clode@RevivalWorld
