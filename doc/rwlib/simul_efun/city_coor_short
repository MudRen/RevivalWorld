名稱：
	city_coor_short() - 回傳城市座標的短敘述。
語法：
	varargs string city_coor_short(int x, int y, string city, int num);
用法：
	回傳一個標準的城市座標短敘述。
參考：
	load_cityroom, arrange_city_location, city_roomfiles
作者：
	Clode@RevivalWorld
