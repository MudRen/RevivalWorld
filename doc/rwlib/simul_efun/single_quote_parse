名稱：
	single_quote_parse() - 解析單引號的囊括字串。
語法：
	string *single_quote_parse(string p);
用法：
	將字串 p 依照單引號所圍的字串範圍進行分解，例如
	
	single_quote_parse("cp 'A file' B");
	
	則會傳回
	
	({
		"cp",
		"A file",
		"B",
	})
參考：

作者：
	Clode@RevivalWorld
